<div class="iferror">
  <% if @book.errors.any? %>
   <div class="errors">
     <h4 class="error-title">2 errors prohibited this book form being saved:</h4>
      <ul>
        <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
      </ul>
   </div>
   <% end %>
</div>
<h2>User info</h2>
<table class="table">
    
  <tbody>
    <tr>
     <%= attachment_image_tag user, :profile_image, :fill, 100, 100 , fallback: "no_image.jpg"%>
    </tr>
    <tr>
     <th>name</th>
     <th><%= user.name %></th>
     <!--ここでエラーが生じていた理由：・bookを投稿したユーザーに変化するため（current_userではない）
     そのため、book controllerからuser情報を取得する必要があるので、books_controllerにuserを定義する必要があった-->
    </tr>
    <tr>
     <th>introduction</th>
     <th><%= user.introduction %></th>
    </tr>
   
  </tbody>
</table>
  
 <div class="section1 text-center">
  <%= link_to edit_user_path(user), class: "col-sm-12 btn btn-outline-secondary btn-block edit_user_#{user.id}" do %>
    <i class="fas fa-user-cog"></i>
  <% end %>
      
 </div>