<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <!-- CSSファイルの読み込み -->
    <link rel="stylesheet" href="books.scss" />
    <title>index</title>
  </head>
<body>
  <main>
   <div class="container px-5 px-sm-0">
    <div class="row">
     <div class="col-sm-3">　<!--部分テンプレ-->
      <%= render 'homes/part' , user: current_user %>
     </div>
     <div class="col-sm-8 offset-md-1">
      <h2 class="main-title">Books</h2>
      <table class="table table-hover table-inverse">
       <thead>
        <tr>
         <th></th>
         <th>Title</th>
         <th>Opinion</th>
        </tr>
        </thead>
        <tbody>
         <% @books.each do |book|%>
           <tr>
            <td>
             <div class="icon-small"></div>
            </td>
            <td>
             <%= link_to book.title , book_path(book.id) %>
            </td>
            <td>
             <%= book.body %>
            </td>
           </tr>
         <% end %>
        </tbody>
      </table>
    </div>
   </div>
  </main>
</body>