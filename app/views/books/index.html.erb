<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <!-- CSSファイルの読み込み -->
    <link rel="stylesheet" href="books.scss" />
    <title>index</title>
  </head>
<body>
  <main>
   <div class="container px-5 px-sm-0">
    <div class="row">
     <div class="col-sm-3">
      <h1>User info</h1>
      <%= form_with model:@user, local:true do |h| %>
      <table class="table">
       <tbody>
        <tr></tr>
        <tr>
         <th>name</th>
         <th><!--ログインユーザー名-->
          <%= %>
         </th>
        </tr>
        <tr>
         <th>introduction</th>
         <th>
          
         </th>
        </tr>
       </tbody>
      </table>
      <div class="row">

      </div>
      <% end %>
      <h1>New book</h1>
       <% if @book.errors.any? %>
        <div class="errors">
         <h4 class="error-title">2 errors prohibited this book form being saved:</h4>
         <ul>
          <% @book.errors.full_messages.each do |message| %>
           <li><%= message %></li>
          <% end %>
         </ul>
        </div>
       <% end %>
       <%= form_with model:@book, local:true do |f|%>
        <p>Title<br /><%= f.text_field :title %></p>
        <p>Body<br /><%= f.text_area :body %></p>
        <br>
       <%= f.submit 'Create Book', class: 'btn btn-success' %>
      <% end %>
     </div>
     <div class="col-sm-8 offset-md-1">
      <h2>Books</h2>
      <table class="table table-hover table-inverse">
       <thead>
        <tr>
         <th></th>
         <th>Title</th>
         <th>Opinion</th>
        </tr>
        </thead>
        <tbody>
         <% @books.each do |g| %>
           <tr>
            <td>
             <%= attachment_image_tag @user, profile_image, :fill, 100, 100, format: 'jpeg',fallback: "no_image.jpg" %>
           <%= g.name %>
            </td>
            <td>
             <%= link_to g.title, edit_book_path(g.id) %>
            </td>
            <td>
             <%= g.opinion %>
            </td>
           </tr>
         <% end %>
        </tbody>
      </table>
    </div>
   </div>
  </main>
</body>